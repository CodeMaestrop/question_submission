potd1
Find the second largest number of the array?
solution:
 code-
    int max=-1;
    int smax=-1;
    for(int i=0;i<nums.size();i++)
    {
      if(a[i]>max)
      {
      smax=max;
      max=a[i];
      }
      if(a[i]!=max&&a[i]>smax)
      smax=a[i];
    }
    return smax;
